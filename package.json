{
  "name": "dungeon-rooms",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx serve -l 6969",
    "build": "node build.mjs",
    "build:fast": "node build.mjs --fast",
    "preview": "node build.mjs && npx serve dist -l 6969"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "javascript-obfuscator": "^5.3.0"
  }
}
